import { addNotification, removeNotification } from './other';
import { getProducts, sendOrder } from './products';

export {
  // products
  getProducts,
  sendOrder,
  // other
  addNotification,
  removeNotification,
};
